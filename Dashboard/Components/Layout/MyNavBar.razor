<div class="flex h-screen bg-gray-100">
    <!-- Sidebar -->
    <aside class="w-64 bg-gradient-to-b from-indigo-700 to-indigo-900 text-white flex flex-col shadow-lg fixed h-full">
        <!-- Logo -->
        <div class="p-6 flex items-center justify-center border-b border-indigo-600">
            <span class="text-2xl font-bold">Gestor TM</span>
        </div>

        <!-- Navegación -->
        <nav class="flex-1 p-4 space-y-2">
            <p class="text-gray-300 uppercase text-xs tracking-wide">Gestión de Ventas</p>
            
            <NavLink class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-indigo-600 transition-colors" href="/dashboard" Match="NavLinkMatch.All">
                <i class="bi bi-box-seam-fill"></i>
                <span>Productos</span>
            </NavLink>

            <NavLink class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-indigo-600 transition-colors" href="/ventas">
                <i class="bi bi-cart-fill"></i>
                <span>Ventas</span>
            </NavLink>

            <NavLink class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-indigo-600 transition-colors" href="/historial">
                <i class="bi bi-clock-history"></i>
                <span>Historial</span>
            </NavLink>

            <NavLink class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-indigo-600 transition-colors" href="/clientes">
                <i class="bi bi-people-fill"></i>
                <span>Clientes</span>
            </NavLink>

            <hr class="border-indigo-600 my-4" />

            <p class="text-gray-300 uppercase text-xs tracking-wide">Gestión de Reparaciones</p>
            
            <NavLink class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-indigo-600 transition-colors" href="/ordenes">
                <i class="bi bi-tools"></i>
                <span>Órdenes</span>
            </NavLink>

            <NavLink class="flex items-center gap-3 px-4 py-2 rounded-lg hover:bg-indigo-600 transition-colors" href="/historial-ordenes">
                <i class="bi bi-journal-check"></i>
                <span>Historial de Órdenes</span>
            </NavLink>
        </nav>

        <!-- Footer -->
        <div class="p-4 border-t border-indigo-600 text-sm text-center text-gray-400">
            © 2025 Gestor TM
        </div>
    </aside>

    <!-- Contenido -->
    <div class="flex-1 ml-64 p-6 overflow-y-auto">
    </div>
</div>
